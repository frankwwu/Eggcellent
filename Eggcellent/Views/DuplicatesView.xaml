<UserControl x:Class="Eggcellent.Views.DuplicatesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls">
    <Grid Margin="28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="Duplicate Finder" Style="{StaticResource SectionTitle}"/>
            <TextBlock Text="Find identical files in a folder and remove the extra copies." Style="{StaticResource SectionSubtitle}"/>
        </StackPanel>

        <Border Grid.Row="1" Style="{StaticResource CardBorder}" Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0" Text="{Binding SelectedFolder}" IsReadOnly="True" mah:TextBoxHelper.Watermark="Folder to scan" VerticalContentAlignment="Center" Margin="0,0,10,0"/>
                <Button Grid.Column="1" Content="Browse..." Command="{Binding BrowseCommand}" Style="{StaticResource PrimaryActionButton}" Margin="0,0,10,0"/>
                <mah:ProgressRing Grid.Column="2" IsActive="{Binding IsBusy}" Width="22" Height="22" Margin="0,0,14,0" Foreground="{DynamicResource MahApps.Brushes.Accent}"/>
                <Button Grid.Column="3" Content="Scan" Command="{Binding ScanCommand}" Style="{StaticResource PrimaryActionButton}"/>
            </Grid>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource CardBorder}" Padding="0">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Groups}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="{StaticResource EggcellentBorder}" BorderThickness="0,0,0,1" Padding="14,10">
                                <StackPanel>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="{Binding SizeDisplay, StringFormat='{}{0} each'}" FontWeight="SemiBold" Foreground="{StaticResource EggcellentTextPrimary}"/>
                                        <TextBlock Grid.Column="1" Text="{Binding WastedDisplay, StringFormat='{}{0} wasted'}" Foreground="{StaticResource EggcellentDanger}" FontWeight="SemiBold"/>
                                    </Grid>
                                    <ItemsControl ItemsSource="{Binding Files}" Margin="0,8,0,0">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Grid Margin="0,3,0,3">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto"/>
                                                        <ColumnDefinition Width="*"/>
                                                    </Grid.ColumnDefinitions>
                                                    <CheckBox Grid.Column="0" IsChecked="{Binding IsSelected}" VerticalAlignment="Center" Margin="0,0,10,0"/>
                                                    <TextBlock Grid.Column="1" Text="{Binding FullPath}" FontSize="12" Foreground="{StaticResource EggcellentTextMuted}" TextTrimming="CharacterEllipsis" VerticalAlignment="Center"/>
                                                </Grid>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <Grid Grid.Row="3" Margin="0,14,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="{Binding Status}" VerticalAlignment="Center" Foreground="{StaticResource EggcellentTextMuted}"/>
            <Button Grid.Column="1" Content="Delete Selected" Command="{Binding DeleteSelectedCommand}" Style="{StaticResource DangerActionButton}"/>
        </Grid>
    </Grid>
</UserControl>
