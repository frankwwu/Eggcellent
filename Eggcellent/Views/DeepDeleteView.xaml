<UserControl x:Class="Eggcellent.Views.DeepDeleteView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls">
    <Grid Margin="28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="File Deep Delete" Style="{StaticResource SectionTitle}"/>
            <TextBlock Text="Permanently destroy sensitive files so they can't be recovered by normal means." Style="{StaticResource SectionSubtitle}"/>
        </StackPanel>

        <Border Grid.Row="1" Style="{StaticResource CardBorder}" Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Content="Add Files..." Command="{Binding AddFilesCommand}" Style="{StaticResource PrimaryActionButton}" Margin="0,0,20,0"/>
                <TextBlock Grid.Column="1" Text="Overwrite passes:" VerticalAlignment="Center" Foreground="{StaticResource EggcellentTextMuted}" Margin="0,0,10,0"/>
                <mah:NumericUpDown Grid.Column="2" Value="{Binding Passes}" Minimum="1" Maximum="7" HorizontalAlignment="Left" Width="100"/>
                <mah:ProgressRing Grid.Column="3" IsActive="{Binding IsBusy}" Width="22" Height="22" Margin="0,0,14,0" Foreground="{DynamicResource MahApps.Brushes.Accent}"/>
                <Button Grid.Column="4" Content="Deep Delete Selected" Command="{Binding ShredCommand}" Style="{StaticResource DangerActionButton}"/>
            </Grid>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource CardBorder}" Padding="0">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Files}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border BorderBrush="{StaticResource EggcellentBorder}" BorderThickness="0,0,0,1" Padding="14,10">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Name}" FontWeight="SemiBold" Foreground="{StaticResource EggcellentTextPrimary}"/>
                                        <TextBlock Text="{Binding FullPath}" FontSize="11" Foreground="{StaticResource EggcellentTextMuted}" TextTrimming="CharacterEllipsis"/>
                                    </StackPanel>
                                    <TextBlock Grid.Column="1" Text="{Binding SizeDisplay}" VerticalAlignment="Center" Margin="0,0,16,0" Foreground="{DynamicResource MahApps.Brushes.Accent}" FontWeight="SemiBold"/>
                                    <Button Grid.Column="2" Content="Remove"
                                            Command="{Binding DataContext.RemoveCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}"
                                            Style="{StaticResource PrimaryActionButton}" Padding="10,5"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <TextBlock Grid.Row="3" Text="{Binding Status}" Margin="0,14,0,0" Foreground="{StaticResource EggcellentTextMuted}"/>
    </Grid>
</UserControl>
